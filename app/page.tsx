"use client";

import { useState } from "react";

type Question = { prompt: string; options: string[]; correct: number; feedback: string; hint: string; challenge?: string };
type Lesson = { id: string; unit: string; title: string; description: string; concept: string; teaching: string; example: string; why: string; takeaways: string[]; questions: Question[] };

const lessons: Lesson[] = [
  { id: "01", unit: "READ DISTRIBUTIONS", title: "Read the shape before the average", description: "Use summaries to notice what the data may hide.", concept: "A distribution is the shape made by all observations. The mean moves with extreme values; the median is more resistant.", teaching: "Compare the mean and median before you call an average typical. A gap between them is an invitation to inspect the full distribution.", example: "Most patients stay 1–3 days, but a small group stays for weeks. The mean is 5 days; the median is 2.", why: "The long stays pull the mean upward. Reporting both values keeps the unusual stays from defining every patient.", takeaways: ["Mean above median often suggests right skew.", "Median is often closer to a typical patient when extremes are high.", "A summary should start a question, not end analysis."], questions: [
    { prompt: "Mean length of stay is 4 days and median is 2 days. What is the strongest interpretation?", options: ["The distribution is perfectly symmetric.", "A few long stays may be pulling the mean upward.", "Every stay was exactly 2 days.", "The median is an error."], correct: 1, feedback: "A smaller number of high values can pull the mean upward while leaving the median closer to a typical stay.", hint: "Which summary moves most when unusually large values appear?", challenge: "A clinic has mean wait time 31 minutes and median 18. What is the strongest interpretation?" },
    { prompt: "Which measure is least affected by one extremely long hospital stay?", options: ["Mean", "Median", "Range", "Maximum"], correct: 1, feedback: "The median depends on the middle position, so one high value affects it much less.", hint: "Think about the middle value after sorting." },
    { prompt: "Why report both mean and median length of stay?", options: ["To hide variation", "To see whether extremes change the average", "Because the values must match", "Because median is categorical"], correct: 1, feedback: "The comparison reveals whether a small group may be distorting the average.", hint: "What does the gap help you notice?" },
    { prompt: "Mean and median both equal 8. What is justified?", options: ["The data are definitely symmetric", "There are no outliers", "The summaries agree, but inspect the full shape", "Every value is 8"], correct: 2, feedback: "Matching summaries are useful clues, not proof that a dataset has no unusual features.", hint: "Can two numbers describe every detail?" }
  ] },
  { id: "02", unit: "DATA TYPES", title: "Name the variable", description: "Recognize labels, order, counts, and measurements.", concept: "Data type determines which summaries and comparisons make sense. Categorical values name groups; ordinal values have order but unequal steps.", teaching: "Say what each field represents before calculating anything. A graph can look polished and still be misleading when it treats a label as a number.", example: "Discharge disposition is recorded as home, skilled nursing facility, hospice, or transferred.", why: "Those values identify groups. They do not form one universal clinical ladder, so the field is categorical.", takeaways: ["Categorical values are labels, not measurements.", "Ordinal values are ordered but step sizes are not known.", "Variable type constrains the claims you can defend."], questions: [
    { prompt: "Discharge disposition is home, skilled nursing facility, or hospice. What kind of variable is it?", options: ["Continuous", "Ordinal", "Categorical", "Binary"], correct: 2, feedback: "Disposition names groups; it is not a universal numeric scale.", hint: "Does each value measure an amount or name a group?", challenge: "Triage is red, yellow, or green. Which kind of field is this?" },
    { prompt: "Pain rated 0 through 10 is commonly treated as which kind of variable?", options: ["Nominal", "Ordinal", "Continuous", "Identifier"], correct: 1, feedback: "The ratings are ordered, but the clinical distance between steps is not guaranteed to be equal.", hint: "The values are ordered, but are their steps equal?" },
    { prompt: "Which field is a count?", options: ["Number of emergency visits", "Blood type", "Discharge disposition", "Patient race"], correct: 0, feedback: "Counts record whole-number events, such as visits, falls, or medication administrations.", hint: "Which answer means 'how many?'" },
    { prompt: "Why does variable type matter before making a graph?", options: ["It determines meaningful comparisons", "It changes diagnosis", "It removes missing values", "It makes every field continuous"], correct: 0, feedback: "Variable meaning determines whether averages, ordering, or group comparisons make sense.", hint: "A histogram and a bar chart do not make the same claim." }
  ] },
  { id: "03", unit: "OUTLIERS", title: "Investigate before removing", description: "Separate unusual observations from impossible values.", concept: "An outlier may be a data error, a measurement issue, or an important clinical event. Context decides which one.", teaching: "Check the source, units, timing, and related fields before correcting or excluding a surprising record.", example: "A record contains creatinine of 14.0, much higher than most values in the file.", why: "It may indicate severe kidney injury. Compare units, repeat labs, diagnosis, and chart context before deciding it is an error.", takeaways: ["Unusual does not automatically mean incorrect.", "Use clinical context and data provenance first.", "Document any cleaning decision."], questions: [
    { prompt: "A patient record has creatinine 14.0. What should you do first?", options: ["Delete it", "Replace it with the mean", "Check context and related fields", "Round it down"], correct: 2, feedback: "A creatinine of 14.0 can be clinically plausible and important. Investigate before changing it.", hint: "Separate an unusual value from an impossible one." },
    { prompt: "What best helps verify a suspicious laboratory value?", options: ["Spreadsheet color", "Units, timestamp, and related results", "Its effect on the mean", "Number of rows"], correct: 1, feedback: "Units, timing, and related evidence help distinguish a real event from a processing error.", hint: "What follows the value from lab to chart?" },
    { prompt: "Why can deleting every outlier be harmful?", options: ["It enlarges the dataset", "It can remove meaningful rare patients", "It blocks charts", "It changes labels to numbers"], correct: 1, feedback: "Values far from the center can be exactly the cases that matter for safety and outcomes.", hint: "What might an extreme value represent besides a mistake?" },
    { prompt: "After correcting a confirmed entry error, what should be recorded?", options: ["Nothing", "Original value, evidence, and decision", "Only the new mean", "A new patient ID"], correct: 1, feedback: "An audit trail makes the cleaning decision transparent and reviewable.", hint: "Could another analyst need to understand the change?" }
  ] },
  { id: "04", unit: "MISSINGNESS", title: "Ask why data are absent", description: "Look for the process behind a blank field.", concept: "Missing data are information about a process. The reason a value is absent can change who is represented in the analysis.", teaching: "Ask who is missing, when values become missing, and whether the care process connects that gap to the outcome.", example: "Blood-pressure values are missing mostly for patients who left the clinic early.", why: "The gaps are tied to workflow and perhaps behavior. Deleting those rows may systematically exclude a meaningful group.", takeaways: ["Missingness can create bias.", "Deleting incomplete rows is not automatically safe.", "The mechanism guides the next analytic step."], questions: [
    { prompt: "Blood pressure is missing mostly for patients who left early. Which statement is most accurate?", options: ["Missing completely at random", "Missingness may be tied to workflow and bias results", "Missingness never affects large samples", "Always delete those rows"], correct: 1, feedback: "Missingness tied to leaving early is unlikely to be random and may change who is represented.", hint: "Could the reason be connected to care process?" },
    { prompt: "What is the best first question when many values are blank?", options: ["Which color should represent blanks?", "Why are values missing, and for whom?", "Can I delete rows today?", "What is the overall mean?"], correct: 1, feedback: "Understand the missingness mechanism before deletion or imputation.", hint: "A blank is evidence about a process." },
    { prompt: "Why might deleting every incomplete row create bias?", options: ["It removes a non-random group", "It guarantees better predictions", "It changes categories", "It removes outliers"], correct: 0, feedback: "Patterned missingness changes the population represented in the result.", hint: "Are removed patients likely to be a random sample?" },
    { prompt: "Which action supports transparent handling of missing data?", options: ["Hide it", "Describe the amount, pattern, and method", "Replace all blanks with zero", "Assume every blank is negative"], correct: 1, feedback: "Readers need to know how much is missing, why it may be missing, and how it was handled.", hint: "What would a careful reviewer need to know?" }
  ] }
];

const diagnostic: Question[] = [
  { prompt: "Which value is least affected by one extremely long stay?", options: ["Mean", "Median", "Range", "Maximum"], correct: 1, feedback: "Median is resistant to a single extreme value.", hint: "Think about the middle value." },
  { prompt: "Is a patient ID a measurement?", options: ["Yes", "No, it is an identifier", "Only if it has digits", "Only if it is unique"], correct: 1, feedback: "An identifier labels a record; it is not a quantity.", hint: "Does it represent an amount?" },
  { prompt: "What happens first when a value looks impossible?", options: ["Delete it", "Check source, units, and context", "Replace with zero", "Ignore it"], correct: 1, feedback: "Investigate provenance before changing a record.", hint: "Could units or context explain it?" },
  { prompt: "Why inspect missing values?", options: ["They may reflect process and bias results", "They are always zero", "They never affect results", "They mean healthy patients"], correct: 0, feedback: "Missingness can be a meaningful pattern.", hint: "A blank tells you something about process." },
  { prompt: "Mean and median differ. What should you do next?", options: ["Assume an error", "Inspect distribution and extremes", "Delete the median", "Report only the mean"], correct: 1, feedback: "The difference is a reason to inspect shape and extremes.", hint: "What could pull one summary away from the other?" }
];

export default function Home() {
  const [screen, setScreen] = useState<"home" | "course" | "lesson" | "diagnostic" | "result" | "notes">("home");
  const [lessonIndex, setLessonIndex] = useState(0);
  const [stage, setStage] = useState<"learn" | "practice" | "complete">("learn");
  const [questionIndex, setQuestionIndex] = useState(0);
  const [choice, setChoice] = useState<number | null>(null);
  const [feedback, setFeedback] = useState<"idle" | "correct" | "incorrect">("idle");
  const [hint, setHint] = useState(false);
  const [completed, setCompleted] = useState<number[]>([]);
  const [unlocked, setUnlocked] = useState(0);
  const [tutorReply, setTutorReply] = useState("");
  const [tutorLoading, setTutorLoading] = useState(false);
  const [diagIndex, setDiagIndex] = useState(0);
  const [diagChoice, setDiagChoice] = useState<number | null>(null);
  const [diagScore, setDiagScore] = useState(0);
  const [level, setLevel] = useState<"Foundation" | "Standard" | "Accelerated">("Foundation");
  const lesson = lessons[lessonIndex];
  const questions = lesson.questions.map(q => level === "Accelerated" && q.challenge ? { ...q, prompt: q.challenge } : q);
  const question = questions[questionIndex];
  const mastery = Math.round(completed.length / lessons.length * 100);

  function openLesson(index: number) { if (index > unlocked) return; setLessonIndex(index); setStage("learn"); setQuestionIndex(0); setChoice(null); setFeedback("idle"); setHint(false); setTutorReply(""); setScreen("lesson"); }
  function startPractice() { setStage("practice"); setQuestionIndex(0); setChoice(null); setFeedback("idle"); setHint(false); }
  function checkAnswer() { if (choice !== null) setFeedback(choice === question.correct ? "correct" : "incorrect"); }
  function nextQuestion() { if (feedback !== "correct") return; if (questionIndex < 3) { setQuestionIndex(questionIndex + 1); setChoice(null); setFeedback("idle"); setHint(false); return; } if (!completed.includes(lessonIndex)) setCompleted([...completed, lessonIndex]); if (lessonIndex < 3) setUnlocked(lessonIndex + 1); setStage("complete"); }
  function retry() { setChoice(null); setFeedback("idle"); setHint(false); }
  function beginDiagnostic() { setDiagIndex(0); setDiagChoice(null); setDiagScore(0); setScreen("diagnostic"); }
  function submitDiagnostic() { if (diagChoice === null) return; const score = diagScore + (diagChoice === diagnostic[diagIndex].correct ? 1 : 0); if (diagIndex < 4) { setDiagScore(score); setDiagIndex(diagIndex + 1); setDiagChoice(null); } else { setDiagScore(score); setLevel(score >= 4 ? "Accelerated" : score >= 2 ? "Standard" : "Foundation"); setScreen("result"); } }
  async function askTutor() { setTutorLoading(true); try { const r = await fetch("/api/tutor", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ question: stage === "practice" ? question.prompt : lesson.concept, learnerAnswer: choice === null ? "I am reviewing this lesson." : question.options[choice], hintLevel: hint ? 1 : 0 }) }); const data = await r.json() as { reply?: string }; setTutorReply(data.reply || "Start by naming the evidence you would use."); } catch { setTutorReply("The tutor is unavailable. Use the lesson and hint to continue."); } finally { setTutorLoading(false); } }

  return <main className="course-app">
    <header className="app-header"><button className="app-brand" onClick={() => setScreen("home")}><span className="brand-orbit"><i /></span><span>Socratic<span>AI</span></span></button><nav><button className={screen === "course" ? "active" : ""} onClick={() => setScreen("course")}>Course</button><button className={screen === "diagnostic" ? "active" : ""} onClick={beginDiagnostic}>Diagnostic</button><button onClick={() => setScreen("notes")}>Notebook</button></nav><div className="header-progress"><span><b>{completed.length}</b> / 4 skills</span><i><b style={{ width: `${mastery}%` }} /></i><button className="profile-dot">AK</button></div></header>

    {screen === "home" && <section className="home-screen"><div className="home-copy"><p className="eyebrow"><i /> BASIC DATA LITERACY FOR CLINICIANS</p><h1>See the signal.<br/><em>Keep your judgment.</em></h1><p className="home-intro">A Socratic learning studio that teaches the idea first, then makes you use it.</p><div className="home-actions"><button className="primary-button" onClick={() => setScreen("course")}>Enter Module 01 <span>&rarr;</span></button><button className="text-button" onClick={beginDiagnostic}>Take the diagnostic <span>&rarr;</span></button></div></div><div className="home-orbit"><p>MODULE<br/>01 / 04</p><div className="orbit-outer"><div className="orbit-middle"><div className="orbit-inner"><strong>DATA<br/>LITERACY</strong></div></div></div><span className="orbit-pulse" /></div><div className="home-footer"><span>01 — PREPARE & EXPLORE</span><p>Learn the concept, work through a clinical example, then earn mastery through four questions.</p><button onClick={() => setScreen("course")}>Explore curriculum <span>↓</span></button></div></section>}

    {screen === "diagnostic" && <section className="diagnostic-screen"><div className="diagnostic-aside"><p className="eyebrow"><i /> SEPARATE PLACEMENT DIAGNOSTIC</p><h1>Find your<br/><em>starting point.</em></h1><p>This five-question check only selects a starting level. It never unlocks or completes a course skill.</p><div className="diagnostic-meter"><span>QUESTION {diagIndex + 1} OF 5</span><i><b style={{ width: `${(diagIndex + 1) * 20}%` }} /></i></div></div><article className="diagnostic-card"><p className="question-type">PLACEMENT CHECK</p><h2>{diagnostic[diagIndex].prompt}</h2><div className="answer-list diagnostic-options">{diagnostic[diagIndex].options.map((x, i) => <button key={x} className={diagChoice === i ? "selected" : ""} onClick={() => setDiagChoice(i)}><span>{String.fromCharCode(65 + i)}</span><p>{x}</p></button>)}</div><button className="check-button" disabled={diagChoice === null} onClick={submitDiagnostic}>Continue <span>&rarr;</span></button></article></section>}

    {screen === "result" && <section className="result-screen"><p className="eyebrow"><i /> DIAGNOSTIC COMPLETE</p><h1>Your starting level:<br/><em>{level}.</em></h1><p>{level === "Accelerated" ? "You demonstrated strong foundations. Where available, the course will use more demanding question wording." : level === "Standard" ? "You have useful foundations. The lessons will build fluency with clear explanations and applied checks." : "You have a clear place to begin. Module 1 will teach each core idea before asking you to use it."}</p><div><span>{diagScore} / 5 correct</span><button className="primary-button" onClick={() => setScreen("course")}>Start Module 01 <span>&rarr;</span></button></div></section>}

    {screen === "course" && <section className="course-screen"><aside className="course-sidebar"><div className="course-kicker">YOUR PATH <span>01 / 04</span></div><h2>Prepare<br/><em>& explore.</em></h2><p>Read the structure of clinical data before you use it to draw a conclusion.</p><div className="mastery-block"><div><span>MODULE MASTERY</span><b>{mastery}%</b></div><i><em style={{ width: `${mastery}%` }} /></i></div><div className="path-legend"><span><i /> Learned</span><span><i /> Locked until prior mastery</span></div><footer><span>●</span> Diagnostic level: {level}</footer></aside><section className="course-main"><p className="eyebrow"><i /> MODULE 01 — FOUNDATIONS</p><h1>Learn first.<br/><em>Then prove it.</em></h1><p className="course-intro">Each skill begins with a concise teaching lesson and a clinical example. You unlock the next skill only after answering all four mastery questions correctly.</p><div className="module-meta"><span>4 teaching lessons</span><span>16 mastery questions</span><span>Adaptive level: {level}</span></div><div className="skill-list">{lessons.map((item, index) => { const locked = index > unlocked; return <button key={item.id} disabled={locked} className={locked ? "locked" : ""} onClick={() => openLesson(index)}><span className={`skill-number ${completed.includes(index) ? "complete" : ""}`}>{completed.includes(index) ? "✓" : locked ? "•" : item.id}</span><div><small>{item.unit}</small><b>{item.title}</b><p>{completed.includes(index) ? "Mastered — review lesson and questions" : locked ? "Complete the previous skill to unlock" : "Teaching lesson + four-question mastery check"}</p></div><em>{completed.includes(index) ? "Review" : locked ? "Locked" : "Begin"} <span>&rarr;</span></em></button>; })}</div></section></section>}

    {screen === "lesson" && <section className="lesson-screen"><aside className="lesson-rail"><button className="rail-back" onClick={() => setScreen("course")}>← <span>Back to module</span></button><div className="rail-mark"><span className="brand-orbit"><i /></span><p>SOCRATIC<br/>TUTOR</p></div><div className="rail-title"><span>MODULE 01</span><h2>{lesson.title}</h2><p>{lesson.description}</p></div><div className="rail-progress"><span>SKILL STATUS</span><b>{stage === "learn" ? "Learn" : stage === "practice" ? `${questionIndex + 1}/4` : "Done"}</b><i><strong style={{ width: `${stage === "learn" ? 18 : stage === "practice" ? 22 + questionIndex * 18 : 100}%` }} /></i></div><div className="rail-list">{lessons.map((item, index) => <button key={item.id} disabled={index > unlocked} className={lessonIndex === index ? "active" : ""} onClick={() => openLesson(index)}><span>{completed.includes(index) ? "✓" : item.id}</span>{item.title}</button>)}</div></aside><section className="lesson-workspace"><div className="lesson-layout"><div className="lesson-content"><div className="lesson-heading"><p className="eyebrow"><i /> {lesson.unit}</p><span>{stage === "learn" ? "TEACHING LESSON" : stage === "practice" ? `MASTERY QUESTION ${questionIndex + 1} OF 4` : "SKILL COMPLETE"}</span></div>{stage === "learn" && <article className="teaching-card"><p className="question-type">THE CORE IDEA</p><h1>{lesson.title}</h1><p className="lesson-lead">{lesson.concept}</p><p className="lesson-body">{lesson.teaching}</p><section className="example-card"><span>CLINICAL EXAMPLE</span><p>{lesson.example}</p><strong>Why it matters</strong><p>{lesson.why}</p></section><section className="takeaways"><span>KEEP THESE IN MIND</span>{lesson.takeaways.map(x => <p key={x}><i>+</i>{x}</p>)}</section><button className="primary-button" onClick={startPractice}>Begin four-question mastery check <span>&rarr;</span></button></article>}{stage === "practice" && <article className="question-card"><p className="question-type">APPLY THE IDEA</p><h1>Check your reasoning.</h1><p className="lesson-prompt">{question.prompt}</p><div className="answer-list">{question.options.map((x, i) => <button key={x} disabled={feedback === "correct"} className={`${choice === i ? "selected" : ""} ${feedback === "correct" && i === question.correct ? "correct" : ""} ${feedback === "incorrect" && choice === i ? "incorrect" : ""}`} onClick={() => feedback === "idle" && setChoice(i)}><span>{String.fromCharCode(65 + i)}</span><p>{x}</p></button>)}</div>{hint && feedback !== "correct" && <div className="hint-box"><b>A nudge, not the answer</b><p>{question.hint}</p></div>}{feedback !== "idle" && <div className={`answer-feedback ${feedback}`}><b>{feedback === "correct" ? "Correct." : "Try again."}</b><p>{feedback === "correct" ? question.feedback : "Use the hint, revisit the teaching card if needed, and make another choice. Mastery requires the correct answer."}</p></div>}<div className="question-actions"><button className="hint-link" onClick={() => setHint(true)}>Need a nudge? <span>+</span></button>{feedback === "incorrect" && <button className="retry-button" onClick={retry}>Try another answer</button>}<button className="check-button" disabled={choice === null} onClick={feedback === "correct" ? nextQuestion : checkAnswer}>{feedback === "correct" ? (questionIndex === 3 ? "Complete skill" : "Next question") : "Check answer"} <span>&rarr;</span></button></div></article>}{stage === "complete" && <article className="complete-card"><p className="question-type">MASTERY EARNED</p><h1>You made the<br/><em>reasoning yours.</em></h1><p>You answered all four questions correctly. The next skill is now unlocked.</p><button className="primary-button" onClick={() => lessonIndex < 3 ? openLesson(lessonIndex + 1) : setScreen("course")}>{lessonIndex < 3 ? "Open next skill" : "Return to Module 01"} <span>&rarr;</span></button></article>}</div><aside className="tutor-card"><div className="tutor-badge"><span>✦</span><p>SOCRATIC TUTOR</p></div><h2>Slow down.<br/><em>Notice more.</em></h2><p>{stage === "learn" ? "Talk through the concept before you begin the mastery check." : "I will give a nudge, challenge the evidence, and leave the answer for you to earn."}</p><button className="tutor-ask" onClick={askTutor} disabled={tutorLoading}>{tutorLoading ? "Thinking..." : "Talk it through"} <span>&rarr;</span></button>{tutorReply && <p className="tutor-reply">{tutorReply}</p>}<div className="tutor-loop"><span>LEARNING LOOP</span><p>Learn <b>→</b> Practice <b>→</b> Master <b>→</b> Apply</p></div></aside></div></section></section>}

    {screen === "notes" && <section className="notes-screen"><p className="eyebrow"><i /> YOUR WORKING NOTEBOOK</p><h1>Make the ideas<br/><em>your own.</em></h1><p>Capture questions, clinical examples, and patterns you want to remember.</p><textarea placeholder="Write the thought you want to keep..." /><span className="notes-tip">TIP — Write one thing you noticed before one thing you learned.</span></section>}
  </main>;
}
